function LoginSubmit(props: any) {
    return (
        <a href="#" onClick={() => {}} className="bg-black text-white px-4 py-2 rounded-full font-bold mt-4 mb-32">{props.children}
        </a>
    )
}

export default LoginSubmit;